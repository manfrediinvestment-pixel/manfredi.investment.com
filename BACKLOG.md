# Backlog — Manfredi Investment

Lista viva de pendientes: bugs, mejoras de diseño, decisiones de producto sin resolver y features a futuro. Se actualiza cada vez que aparece algo nuevo mientras se trabaja en otra tarea, no solo se menciona en el chat.

Formato por item: descripción corta · prioridad (alta/media/baja) · estado (pendiente/en progreso/hecho).

---

## Decisiones de producto

| # | Descripción | Prioridad | Estado |
|---|---|---|---|
| 1 | **Login gate**: se sacó el modal de login a pantalla completa. Ahora el login solo se pide de forma contextual al entrar a contenido premium (Inversiones, Warren), vía el gate suave existente (preview parcial + tarjeta de suscripción). Informes y Asesoría pasaron a ser 100% libres (decisión explícita, ya no son premium). Queda un botón discreto "Ingresar" en el navbar. | Alta | Hecho |
| 2 | Manfredi University: decidir si mantiene fondo claro/editorial como excepción intencional al resto del sitio (terminal oscuro) o si se unifica también. No es automático, requiere decisión explícita. | Media | Pendiente |

## Bugs

| # | Descripción | Prioridad | Estado |
|---|---|---|---|
| 3 | Modal de login (`#loginOverlay`) aparecía automáticamente a pantalla completa al hacer scroll como guest. Se eliminó el trigger por scroll y el intercept de clicks en nav — el login ahora es siempre voluntario o contextual a contenido premium. | Media | Hecho |
| 4 | Contenido de curso duplicado: bloques de "Calculá tu presupuesto en dólares CCL..." y cards de ahorro para el retiro aparecen dos veces en `index.html` (líneas ~4527/6838 y ~4616/6927). | Baja | Pendiente |
| 5 | Encoding roto en íconos de `CAL_DETAILS` (calendario económico) | Alta | Hecho |
| 20 | Encoding roto en el modal de pago: botón "Pagar con Mercado Pago" y aviso de seguridad se veían con mojibake (mismo bug que el ítem 5, en otra parte del archivo). Encontrado al validar visualmente el nuevo CTA de membresía. | Alta | Hecho |
| 25 | Modal de membresía (`#paymentOverlay`) renderizaba título/precio/botón duplicados y superpuestos. Causa real: CSS corrupto (llaves de cierre faltantes en `.instrument-card`/`.instrument-icon` + una llave de más en `.member-lock-fine`) hacía que el parser del navegador descartara las reglas reales de `.payment-overlay`/`.payment-modal-box` como parte de un selector anidado inválido — el modal solo se veía "andando" por una regla `.payment-overlay.open` duplicada y mal ubicada dentro del CSS de `.feed-item` (que además forzaba `.feed-item{display:none}` sitewide). Se corrigieron ambas corrupciones y se eliminó la regla duplicada. | Alta | Hecho |
| 26 | Efecto colateral del ítem 25: al arreglar el CSS corrupto se reactivó sin querer un sistema de blur/paywall legacy (`.member-protected.locked::after` + `.member-lock-*`, sin contraparte en el HTML actual) que duplicaba el gate de Inversiones/Warren por encima del sistema real (JS `applyPaywall`/`paywallCardHTML`), volviendo a blurear título y aviso legal que debían quedar visibles (ver ítem 18). Detectado al validar con Playwright antes de dar por cerrado el ítem 25. Se eliminó el CSS legacy. | Alta | Hecho |
| 27 | `--font-display` y `--bg-card` se usaban en varias reglas (modal de pago, member-lock, etc.) pero nunca estaban definidas en `:root` — caían silenciosamente al valor por defecto del navegador. Se definieron ambas. | Media | Hecho |
| 28 | Filtros de Mercados (AR/US/Crypto/Commod.): las 6 tarjetas superiores (`dash-grid`) nunca se filtraban al tocar un tab — no había JS para eso, solo las tablas de abajo reaccionaban, dando la sensación de layout "congelado". Además, cuando un filtro dejaba una sola tarjeta visible en un grid de 2 columnas fijas, la columna vacía no colapsaba y quedaba un hueco grande sin usar. Se agregó filtrado por `data-region` a las dash-cards y se pasó `.dash-grid`/`.at-grid` a `auto-fit`/flex con columnas con clase propia (`.at-col`) que colapsan cuando quedan vacías. | Alta | Hecho |
| 29 | Errores de consola pre-existentes (no introducidos en esta sesión, detectados al validar con Playwright): `RSFP`/`RSIB`/`RSMA` (renderers de cursos de University) tiran `TypeError: Cannot set properties of null (setting 'innerHTML')` al navegar — mismo tipo de bug que ya se había parchado parcialmente para otros cursos (`slideWrapper`, ver historial de commits) pero no para estos tres. Sparklines de picks con paths SVG inválidos (`<path d="...">` cortado) también tiran error de consola. No se tocó en esta sesión, fuera de alcance de las tareas pedidas. | Media | Pendiente |
| 34 | **Resuelto (2026-07-24):** `.github/workflows/daily-reports.yml` se llama "Reportes Diarios" pero el cron real era `0 11 * * 1` — solo corría los **lunes**, no todos los días (`reports/argentina.json` llevaba 9 días sin refrescar). Cambiado a `0 11 * * *` (todos los días, 8am ART). | Alta | Hecho |
| 38 | Verificado en vivo (2026-07-24) que el worker `manfredi-noticias` (ítem 32) sí se actualiza cada 30 min como estaba diseñado — no es lo mismo que el ítem 34 (son dos pipelines separados). El problema real era volumen: solo 3 noticias de Argentina y 6 de Wall Street llegaban al sitio, muy por debajo del máximo configurado por fuente. Además se confirmó (3 refrescos forzados seguidos) que **Infobae devuelve 0 items de forma consistente** al pedirse desde IPs de Cloudflare Workers — mismo feed funciona perfecto pedido desde afuera, probable bloqueo anti-bot del sitio. Se subieron los topes por fuente (Ámbito 2→4, Cronista 2→4, Bloomberg Línea 1→2, Infobae se deja en 2 como bonus best-effort; Yahoo 2→3, Investing 2→3, Seeking Alpha 1→2, Economist 1→2) para llegar a ~10 por sección aun si Infobae sigue sin responder. | Alta | Hecho |
| 37 | **Resuelto (2026-07-24):** declaraciones CSS sueltas (`--slate: #1e40af; --slate-light: #dbeafe;`) fuera de cualquier selector, entre `.nav-btn:disabled` y `.meta-item` (CSS de cursos). Resto de un `:root` viejo que se había borrado a medias, sin ningún `var(--slate...)` referenciándolas en todo el archivo. Eliminadas. | Baja | Hecho |

## Diseño

| # | Descripción | Prioridad | Estado |
|---|---|---|---|
| 6 | Migrar `#mercados` a fondo oscuro, consistente con Informes/Warren/Inversiones. Incluyó reajustar los colores de acento de las dash-cards (MERV, SPX, NDX, BTC, MEP, XAU) que eran ilegibles sobre navy oscuro. | Alta | Hecho |
| 19 | Navegación por pestañas: una sola sección visible a la vez (Inicio/Mercados/Calendario/Informes/University/Inversiones/Asesoría), con hash propio por sección (`#mercados`, etc.), botón atrás y deep-links funcionando. Se encontró y corrigió un bug de integración con AOS (animaciones fade-up) que dejaba el contenido de una pestaña recién activada invisible. | Alta | Hecho |
| 7 | **Parcialmente resuelto (2026-07-24):** se eliminó el único uso de `Playfair Display` (`.mi-ticker`, el ticker grande del modal de tesis de acciones) y se reemplazó por `DM Serif Display` (ya cargada sitewide vía `<link>`, sin necesidad de un `@import` extra), consistente con el resto de los headings serif del sitio. La segunda parte del ítem original —borrar el `@import` de `Inter`— **no se hizo**: el enunciado del ítem estaba desactualizado, `Inter` sí se usa (es la fuente base de `.mi-panel`, heredada por texto real del modal como la tesis resumida y los casos alcista/bajista, que no tienen `font-family` propia). Verificado visualmente que el modal renderiza igual. | Media | Hecho |
| 8 | Extender `IBM Plex Mono` a todo número en tabla/card del resto del sitio (precios de mercado, KPIs de calendario, precios de picks) — hoy solo vive en el modal de tesis de acciones. | Media | Pendiente |
| 9 | Auditar y reemplazar hex literales sueltos por tokens (`--gold`, `--primary`, etc.), adoptando la escalera de navys del modal `.mi-*` (`#07101E → #0A0F1E → #0a1525 → #0C1828 → #1a2f4a`) como sistema único de elevación. Queda pendiente el audit de los ~160 hex restantes: son colores pasados a configs de Chart.js/canvas (`backgroundColor`, `borderColor`, `plugins.legend`, `scales.ticks`, arrays de colores) donde `var(--token)` no se resuelve (canvas no tiene contexto de cascada CSS) — no tocar sin repensar esos casos puntualmente — más el CSS/JS propio de los cursos de University (paleta separada, decisión de diseño pendiente, ver ítem 2) y colores puntuales del modal `.mi-*` que son acentos (ej. `#3b82f6`/`#60a5fa`/`#4ade80`/`#f87171`), no parte de la escalera de elevación. | Alta | Pendiente |
| 9b | **Resuelto (2026-07-24):** el segundo bloque `:root` de los cursos de University (~línea 4253) redefinía `--red`/`--green`/`--border` globalmente, pisando los tokens reales del sitio. Al auditarlo se encontró que el mismo bloque sin escopar **también** pisaba `body{font-family,background,color}` de todo el sitio (Georgia serif + fondo celeste en vez de IBM Plex Sans + navy), no solo las 3 variables ya documentadas. Se movieron todas las variables + `font-family`/`color` a `.app` (contenedor real de los cursos, confirmado por `.course-modal .app{...}` ya existente en el CSS principal) y se eliminó el reset duplicado (`*,*::before,*::after`, idéntico al global, inofensivo pero redundante). Verificado visualmente en local que el resto del sitio y los cursos mantienen su estilo esperado. | Alta | Hecho |
| 9c | **Resuelto (2026-07-24):** primera pasada mecánica del audit de hex literales, acotada a los dos bloques `<style>` reales del sitio (CSS global + modal `.mi-*` de tesis de acciones; se dejó afuera el CSS propio de los cursos de University por ser un sistema de diseño claro/editorial intencionalmente separado, ver ítem 2). Se agregaron 4 tokens nuevos (`--navy-1/3/4/5`) para la escalera de navys del modal `.mi-*` (el rung 2, `#0A0F1E`, ya era `--primary`) y se reemplazaron 83 ocurrencias de hex literales que duplicaban exactamente un token existente (`--gold`, `--green`, `--red`, `--primary`, `--white`, `--yellow`, `--text-sec`, `--border`, más los 4 nuevos de navy) por su `var()` correspondiente. También se eliminó `--bg-card` (`#0D1526`), un token muerto que no se usaba en ningún lado. Verificado visualmente con Playwright/Chrome (Mercados, Informes, Inversiones y el modal `.mi-*` forzado con backdrop `.active`) que ningún color cambió. | Alta | Hecho |
| 9d | **Resuelto (2026-07-24):** segunda pasada del audit de hex literales, esta vez sobre atributos `style=""` inline en HTML estático y strings de JS que arman `style="..."` (`.style.cssText`, `.innerHTML` con markup, asignaciones directas `.style.propiedad=`) — es decir, todo lo que termina siendo un atributo DOM real (donde `var()` sí resuelve), a diferencia de los colores que van a Chart.js/canvas (que se dejaron intactos a propósito, ver ítem 9). Se reemplazaron 35 hex literales exactos por sus tokens/vars correspondientes en: el dot verde de "Asesor IA" del navbar, `--spot-accent`/`--card-accent` de Mercados, el fondo de la sección Calendario, el quiz de perfil de inversor completo (fondo, barra de progreso, botones y opciones seleccionadas), el card gratis de University, el link de error del visor de PDF, el título del gráfico de Warren, la tarjeta de paywall (`paywallCardHTML`) y tres bloques del modal `.mi-*` (proyecciones, catalizadores, valoración vs peers). De paso se corrigió una regresión propia: el primer intento había anidado `var(--primary,var(--primary))` en vez de dejar el fallback literal `var(--primary,#0A0F1E)` que ya traía el código (un fallback anidado en sí mismo no sirve como fallback). Verificado visualmente con Playwright/Chrome: Mercados, Calendario, el quiz completo (con una opción seleccionada) y sin errores de consola nuevos (los 4 preexistentes del ítem 29 siguen igual). | Alta | Hecho |
| 9e | **Resuelto (2026-07-24):** tercera pasada del audit de hex literales, sobre atributos SVG `stroke=""`/`fill=""` (presentation attributes, que también resuelven `var()` vía cascada CSS — confirmado en vivo con Chrome antes de tocar nada) y dos casos de `style=""` armados por concatenación de string que las dos pasadas anteriores no habían detectado. Se reemplazaron 26 hex literales exactos en los sparklines SVG del hero, las dash-cards de Mercados y las tarjetas de picks de Inversiones, más los 2 colores del gráfico de riesgo del hero (`isUp ? red : green`) y los 3 colores de perfil del resultado del quiz. También se agregó un token nuevo, `--gold-dark` (`#B8943F`), para un dorado oscuro usado de forma consistente (4 veces) en los labels de sección de Informes ("Tipo de cambio", "Merval", "Índices", "Commodities") que no tenía token propio. Al hacer el reemplazo global de `#B8943F` sin acotar por contexto se pisó por error tanto la propia definición del token en `:root` (quedó `--gold-dark: var(--gold-dark)`, autoreferencia rota) como un array de colores de Chart.js (línea ~9480, usado por los gráficos de Warren) — los dos se detectaron en la revisión del diff antes de commitear y se revirtieron a su hex original. Verificado visualmente con Playwright/Chrome: home (sparklines), Mercados (dash-cards), Informes (labels dorados), resultado del quiz (perfil "Inversor Agresivo" con textos en verde), sin errores de consola nuevos. | Alta | Hecho |
| 10 | Sistema de "flash on update" para datos en vivo (precios, KPIs): flash breve de color direccional al cambiar un valor, decae en ~600–900ms, sobre tabular-nums para que no salte el ancho. Implementado para el número de Riesgo País del hero (ítem 31); falta extender a Mercados/Calendario/resto del sitio. | Alta | En progreso |
| 11 | Soporte de `prefers-reduced-motion` — ausente en todo el CSS. Agregado para las animaciones nuevas del hero (flash de Riesgo País, hover magnético del CTA); falta auditar y agregar al resto del sitio. | Baja | En progreso |
| 12 | Letter-spacing progresivo por tamaño de tipografía (hoy ad-hoc, sin regla consistente). | Baja | Pendiente |
| 21 | Estado activo de las pestañas de nav: pasa de texto dorado + subrayado a un pill con borde/texto azul, igual que el botón "Asesoría" del navbar. | Media | Hecho |
| 22 | Botón "Ingresar" del navbar: pasa de ghost dorado improvisado a outline consistente con el resto de los botones del sitio (colores neutros, no dorado). | Media | Hecho |
| 23 | Hero / "Mercados ahora": se sacaron los íconos de bandera/emoji por fila y se reemplazaron por un acento de color fino (reusa la paleta ya definida en Mercados: MERV azul, SPX verde, BTC ámbar, MEP gris). Ticker a IBM Plex Mono. | Media | Hecho |
| 24 | Franja "+10 Reportes Diarios / +25 Activos..." eliminada de la home; reemplazada por un banner de membresía (Warren + Inversiones) con CTA a `openPaymentModal()`. | Alta | Hecho |
| 30 | Rediseño del modal de membresía (fintech premium, post fix del ítem 25): eyebrow + headline propio ("Todo el análisis, sin límites"), un solo precio destacado, lista de 3 beneficios concretos con check en vez del precio suelto, un solo CTA dorado principal ("Suscribirme ahora") y Mercado Pago degradado a aviso de pago seguro secundario (ya no un botón que compite visualmente con el precio). | Alta | Hecho |
| 31 | Rediseño de la pantalla principal (hero): headline más grande y con más aire (clamp 44–76px, tracking -0.02em, sin salto de línea forzado). La tarjeta "Mercados ahora" pasa de 4 filas parejas a un número hero — Riesgo País (EMBI+), a pedido explícito en vez de Merval/S&P — con gráfico de área de datos históricos reales (`api.argentinadatos.com`, no hardcodeado) + 3 filas secundarias (Merval, S&P 500, Bitcoin). Flash direccional de color al actualizar el valor (rojo si sube el riesgo, verde si baja) refrescado cada 90s. Micro-interacción magnética en el CTA principal, acotada a ese único botón. | Alta | Hecho |
| 36 | Fila de meta del header de cursos ("8 lecciones", "Gráficos interactivos", etc.) se veía casi ilegible sobre el degradé azul — `opacity:0.8` sobre texto heredado. Se reemplazó por `color:rgba(255,255,255,.92)` + `font-weight:500` explícitos en las dos definiciones de `.meta-item` (una por curso, 4 cursos comparten la misma clase). | Media | Hecho |

## Features futuros

| # | Descripción | Prioridad | Estado |
|---|---|---|---|
| 13 | Dashboard personalizado / watchlist: usuario logueado fija 5-8 activos de las tablas de Mercados, persistidos por usuario, mostrados primero. | Media | Pendiente |
| 14 | Command palette (⌘K) para búsqueda global sobre calendario, tablas, informes, picks y cursos. | Media | Pendiente |
| 15 | Sparklines reales con Chart.js en picks de Inversiones (hoy son `<polyline>` con puntos hardcodeados, no representan datos reales). | Media | Pendiente |
| 16 | Rediseño de jerarquía tipográfica del modal de tesis de acciones (mismo contenido, un número hero por vista). | Media | Pendiente |
| 17 | Sistema de alertas/notificaciones (ej. "avisame si el riesgo país cambia más de X" o antes de un evento del calendario). | Media | Pendiente |
| 18 | Paywall más honesto con preview real: Inversiones ya mostraba el primer pick sin blur + tarjeta de suscripción; ese mismo gate ahora también se aplica a Warren (antes no se activaba para usuarios anónimos que nunca habían intentado loguearse). Informes/Asesoría dejaron de necesitarlo al pasar a ser libres (ver item 1). | Media | Hecho |
| 32 | Worker `manfredi-noticias` (patrón cache-aside de `manfredi-calendario`, KV + cron cada 30 min, TTL 45 min): agrega Ámbito/Infobae/Cronista/Bloomberg Línea (Argentina) y Yahoo Finance/Investing/Seeking Alpha/The Economist (Wall Street), devuelve `{argentina, wallstreet, updated}` en `/noticias`. `index.html` conectado (`loadDailyReports` pide al Worker en paralelo, usa sus noticias si responde ok, fallback a `reports/*.json` si falla). KV namespace creado (`06e9b3baeb9043fbb60b553cc7be38ed`) y Worker deployado en `https://manfredi-noticias.nachito2502.workers.dev` — verificado respondiendo con datos reales en producción. | Alta | Hecho |
| 35 | **Incidente de seguridad (2026-07-22, encontrado y corregido en la misma sesión):** el deploy manual del Worker de noticias se corrió sin `--config` desde el directorio equivocado y terminó ejecutando `wrangler deploy` contra el `wrangler.jsonc` de la raíz (proyecto Workers-with-assets `manfredinvestment`), que no tenía `.assetsignore`. Eso subió el repo completo como archivos estáticos públicos en `manfredinvestment.nachito2502.workers.dev`, incluyendo `.git/` completo y `.claude/settings.local.json`. Se detectó de inmediato con curl, se creó `.assetsignore` (excluye `.git`, `.claude`, `.agents`, `.wrangler`, `workers`, `scripts`, `node_modules`, docs internas) y se re-deployó — confirmado con reintentos y headers no-cache que esas rutas vuelven a dar 404. Se auditó `git log --all -p` buscando patrones de secretos (API keys, private keys, tokens) y no se encontró ninguno real. El dominio de producción real (`manfredinvestment.com`, servido por Cloudflare Pages) nunca estuvo expuesto — se verificó que su respuesta 200 en rutas tipo `/.git/config` es el fallback SPA normal (mismo contenido que cualquier ruta inexistente), no el archivo real. | Alta | Hecho |
| 33 | Rediseño de `#reportes` (Informes & Reportes): se sacó el chrome de "ventana de terminal" (puntitos semáforo) y se reemplazó por un header editorial (bandera + título serif + badge LIVE). Las variables pasaron de una grilla de 2 columnas apretada a tiles tipo dashboard (`grid-template-columns: repeat(auto-fit, minmax(110px,1fr))`), con Dólar Blue y S&amp;P 500 destacados un escalón por encima del resto (span 2 columnas, 28px vs 20px) por ser las variables que más consulta la audiencia. Noticias con más padding vertical (18px) y line-height (1.65), y tag de fuente (Ámbito/Infobae/etc.) cuando viene del Worker nuevo (item 32). | Alta | Hecho |

---

Detalle y justificación de cada item en `REDESIGN_PLAN.md`. Este archivo es el índice accionable; el plan es el research/racional detrás.
